# private data (persona = 用户真实发言)
luma_dataset/persona_seed/
luma_dataset/**/persona_seed/
luma_dataset/synthetic/persona_private.jsonl
luma_dataset/mixes/
minimind_runtime_dynamics/dataset/
**/dataset/private*/
**/persona_seed/

# large data / external datasets
data/
luma_dataset/synthetic/*.jsonl
!luma_dataset/synthetic/arc_agi.jsonl

# weights / checkpoints
*.pth
*.pt
*.bin
*.safetensors
*.ckpt
minimind/checkpoints/
minimind/out/
**/checkpoints/
**/out/
**/weights/

# env / caches
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.cache/
**/.cache/
.DS_Store

# local experiment noise
*.log
wandb/

# local artifacts not meant for sharing by default
minimind/.wheels/
minimind_runtime_dynamics/.wheels/

# large metrics jsonl (GitHub warns >50MB)
minimind/artifacts/refactor/phase4_metrics.jsonl
minimind/artifacts/refactor/phase6_metrics.jsonl

# experiment artifacts (只保留 refactor/ 和 experiment_matrix 的轻量日志)
minimind/artifacts/autoresearch*/
minimind/artifacts/smoke_*/
minimind/artifacts/luma_dynamics_*/
minimind/artifacts/_launch_*/
minimind/artifacts/arc_agi_local/
minimind/artifacts/longrun_*/
minimind/artifacts/*_2048/
minimind/artifacts/rerun_*/

# trainer symlinks to mixes
minimind/dataset/pretrain_*.jsonl

# misc temp
autoresearch-state.json
research-results.tsv

# local compiled binaries
**/*.cpython-*.so
third_party/mamba-official/*.so
minimind/artifacts/refactor/phase6_metrics.jsonl
